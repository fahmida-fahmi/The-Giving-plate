import React from 'react';

const Title = ({title}) => {
    return (
        <div>
            <h1 className='text-center text-[36px] font-bold my-20 text-white'>{title}</h1>
        </div>
    );
};

export default Title;